<div class="row">
  <div class="col-md-4 mx-auto mt-5 col col-sm-6 col-xs-12">
    <div class="card p-3">
      <h6 class="text-center">Register with OTP</h6>
      <hr/>
      <%= form_for(:register, url: storefront_registers_path) do |f| %>
        <div class="form-group">
          <%= f.hidden_field :email, value: @register_params[:email], class: 'form-control' %>
        </div>
        <div class="form-group">  
          <%= f.hidden_field :mobile, value: @register_params[:mobile], class: 'form-control' %>
        </div>
        <div class="form-group">
          <%= f.label :otp %>
          <%= f.text_field :otp, autofocus: true, autocomplete: 'name', class: 'form-control' %>
        </div>
        <div class="actions">
          <%= f.submit t('auth.registrations.new.action'), class: 'btn btn-primary btn-block' %>
        </div>
      <% end %>
    </div>
  </div>
</div>
